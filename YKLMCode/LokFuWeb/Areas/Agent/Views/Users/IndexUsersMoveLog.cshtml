@{
    List<UsersMoveLog> UsersMoveLogList = ViewBag.UsersMoveLogList;
    SysAgent BasicAgent = ViewBag.BasicAgent;
    this.Layout = null;
}
    <table class="table table-striped table-bordered table-hover tablethc" id="UsersMoveLog">
        <thead>
            <tr>
                <th>真实姓名 </th>
                <th>调出代理商 </th>
                <th>调入代理商 </th>
                <th>操作时间 </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in UsersMoveLogList)
            {
                <tr class="odd gradeX">
                    <td class="center">@(item.UTrueName)</td>
                    <td class="center">@(item.FromName) </td>
                    <td class="center">@(item.ToName) </td>
                    <td class="center">@(item.AddTime.ToString("yyyy-MM-dd HH:mm")) </td>
                </tr>
            }
        </tbody>
    </table>
